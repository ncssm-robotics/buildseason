# Database
# For local dev, use turso dev (recommended - same stack as production):
# 1. Install Turso CLI: brew install tursodatabase/tap/turso
# 2. Start local server: turso dev --db-file local.db
# 3. Use this URL:
DATABASE_URL=http://127.0.0.1:8080

# For Turso cloud (production), set to your database URL:
# DATABASE_URL=libsql://your-db-name.turso.io
# And provide the auth token:
TURSO_AUTH_TOKEN=

# Alternative: plain SQLite file (no turso CLI needed, but different from prod):
# DATABASE_URL=file:local.db

# Auth
# Generate a random secret for local dev: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# GitHub OAuth
# 1. Go to https://github.com/settings/developers
# 2. Click "New OAuth App"
# 3. Set Application name: buildseason-localhost
# 4. Set Homepage URL: http://localhost:3000
# 5. Set Authorization callback URL: http://localhost:3000/api/auth/callback/github
# 6. Click "Register application"
# 7. Copy Client ID below, then click "Generate a new client secret" and copy it
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create a project (or select existing)
# 3. Click "Create Credentials" > "OAuth client ID"
# 4. Select "Web application"
# 5. Add Authorized JavaScript origins: http://localhost:3000
# 6. Add Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
# 7. Copy Client ID and Client Secret below
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Server
PORT=3000
NODE_ENV=development

# Frontend URL (for OAuth callback redirects in development)
# In production, leave empty or set to same as BETTER_AUTH_URL
FRONTEND_URL=http://localhost:5173
