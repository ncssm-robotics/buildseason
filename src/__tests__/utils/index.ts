/**
 * Test utilities for BuildSeason
 *
 * @example
 * ```tsx
 * import { renderWithProviders, screen } from "@/__tests__/utils";
 *
 * test("example", () => {
 *   renderWithProviders(<MyComponent />);
 *   expect(screen.getByRole("button")).toBeInTheDocument();
 * });
 * ```
 */
export * from "./render";
